# משתני סביבה ללוקאלית - חיבור ל-Vercel KV
# ⚠️ חשוב: זה מחליף את ה-localStorage ב-local-kv.js

# Vercel KV credentials - יש לקבל מ-Vercel Dashboard
KV_URL=your_kv_url_here
KV_REST_API_URL=your_rest_api_url_here
KV_REST_API_TOKEN=your_rest_api_token_here
KV_REST_API_READ_ONLY_TOKEN=your_read_only_token_here

# Upstash Redis (alternative)
UPSTASH_REDIS_REST_URL=your_upstash_url_here
UPSTASH_REDIS_REST_TOKEN=your_upstash_token_here

# Admin token for sensitive operations
ADMIN_TOKEN=your_admin_token_here

# ⚠️ אזהרה: הפעלת קובץ זה תגרום למערכת להשתמש ב-Vercel KV גם בלוקאלית
# ודא שיש לך גיבוי מלא של הנתונים לפני ההפעלה!

# הוראות:
# 1. העתק את התוכן לקובץ .env.local
# 2. החלף את הערכים ב-your_*_here בערכים האמיתיים מ-Vercel Dashboard
# 3. הפעל מחדש את השרת
